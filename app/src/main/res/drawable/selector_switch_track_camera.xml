<?xml version="1.0" encoding="utf-8"?><!-- 按钮的选择器，可以设置按钮在不同状态下的时候，按钮不同的颜色 -->
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <item android:state_checked="true">
        <shape android:shape="rectangle">
            <solid android:color="@color/theme_translucent" />

            <size android:width="90dp" android:height="30dp" />
            <corners android:radius="30dp" />
            <stroke android:width="1dp" android:color="@color/theme_color" />
        </shape>


    </item>
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/theme_translucent" />
            <size android:width="90dp" android:height="30dp" />
            <corners android:radius="30dp" />
            <stroke android:width="1dp" android:color="@color/theme_color" />
        </shape>
    </item>
</selector>